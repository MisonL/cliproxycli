services:
  cli-proxy-api:
    image: cliproxy-api:local
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
        COMMIT: ${COMMIT:-none}
        BUILD_DATE: ${BUILD_DATE:-unknown}
    container_name: cli-proxy-api
    # env_file:
    #   - .env
    environment:
      DEPLOY: ${DEPLOY:-}
    ports:
      - "${BIND_ADDRESS:-0.0.0.0}:8317:8317"
      - "${BIND_ADDRESS:-0.0.0.0}:8085:8085"
      - "${BIND_ADDRESS:-0.0.0.0}:1455:1455"
      - "${BIND_ADDRESS:-0.0.0.0}:54545:54545"
      - "${BIND_ADDRESS:-0.0.0.0}:51121:51121"
      - "${BIND_ADDRESS:-0.0.0.0}:11451:11451"

    volumes:
      - ./config.yaml:/CLIProxyAPI/config.yaml
      - ./auths:/CLIProxyAPI/auths
      - ./data:/CLIProxyAPI/data
      - ./logs:/CLIProxyAPI/logs
    restart: unless-stopped
